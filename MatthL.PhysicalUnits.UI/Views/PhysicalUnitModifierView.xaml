<UserControl x:Class="MatthL.PhysicalUnits.Views.PhysicalUnitModifierView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MatthL.PhysicalUnits.Views"
             xmlns:enums="clr-namespace:MatthL.PhysicalUnits.Enums"
             xmlns:converters="clr-namespace:MatthL.PhysicalUnits.Enums" xmlns:boolean="clr-namespace:Mechatro.WPF.UI.Converters.Boolean;assembly=Mechatro.WPF.UI.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="800" x:Name="physicalUnitModifierView">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Mechatro.WPF.UI.Styles;component/StyleDictionary.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <boolean:BoolToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
            <boolean:BoolToInverseConverter x:Key="BooleanToInverseConverter"/>
            <!-- Style pour les labels -->
            <Style x:Key="LabelStyle" TargetType="TextBlock">
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Margin" Value="5,5,10,5"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="HorizontalAlignment" Value="Right"/>
            </Style>

            <!-- Style pour les valeurs -->
            <Style x:Key="ValueStyle" TargetType="TextBlock">
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="Margin" Value="5"/>
            </Style>

            <!-- Style pour les headers de colonne -->
            <Style x:Key="ColumnHeaderStyle" TargetType="TextBlock">
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="HorizontalAlignment" Value="Center"/>
                <Setter Property="Margin" Value="5,5,5,10"/>
                <Setter Property="Foreground" Value="#2053C4"/>
            </Style>

            <!-- Style pour le border principal -->
            <Style x:Key="MainBorderStyle" TargetType="Border">
                <Setter Property="BorderBrush" Value="#DDDDDD"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="CornerRadius" Value="5"/>
                <Setter Property="Padding" Value="10"/>
                <Setter Property="Background" Value="#FAFAFA"/>
            </Style>

            <!-- Style pour les séparateurs de colonnes -->
            <Style x:Key="ColumnSeparatorStyle" TargetType="Border">
                <Setter Property="Width" Value="1"/>
                <Setter Property="Background" Value="#DDDDDD"/>
                <Setter Property="Margin" Value="5,0"/>
            </Style>

           
        </ResourceDictionary>
    </UserControl.Resources>

    <Border Style="{StaticResource MainBorderStyle}" 
            DataContext="{Binding PhysicalUnitViewModel, ElementName=physicalUnitModifierView}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- En-tête avec informations globales -->
            <Border Grid.Row="0" 
                    Background="#E8EFFF" 
                    CornerRadius="5" 
                    Padding="10" 
                    Margin="0,0,0,10">

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="{Binding Name}"
           d:Text="Candela"
                                   
           FontSize="18" 
           FontWeight="Bold" 
           HorizontalAlignment="Center"
           Margin="0,0,0,5" Grid.ColumnSpan="2"/>
                        <TextBlock Grid.Row="1" Text="Symbole" FontSize="10" Foreground="Gray"/>
                    <TextBlock  Grid.Row="1" Text="{Binding Symbol}"
                                       d:Text="cd"
                                      FontFamily="Consolas" 
                                FontSize="14" 
                                      Grid.Column="1"/>
              
                           <TextBlock Text="Type d'unité" FontSize="10" Foreground="Gray" Grid.Row="2"/>
                            <TextBlock Text="{Binding UnitTypeName}"
                                       d:Text="Light"
                                            FontFamily="Consolas" 
FontSize="14" Grid.Row="2" Grid.Column="1"/>
                        

                           <TextBlock Text="Formule " FontSize="10" Foreground="Gray" Grid.Row="3"/>
                            <TextBlock Text="{Binding DimensionalFormula}" 
                                     d:Text="l"
                                       FontFamily="Consolas" 
                                      FontSize="14" Grid.Row="3" Grid.Column="1"/>
                    <TextBlock Text="SiUnit" FontSize="10" Foreground="Gray" Grid.Row="4"/>
                    <TextBlock Text="{Binding SIUnit}" 
                                     d:Text="cd"
                                           FontFamily="Consolas" 
FontSize="14"  Grid.Row="4" Grid.Column="1"/>
                </Grid>
            </Border>

            <!-- Labels -->
            <UniformGrid Grid.Row="2" Columns="1" Width="Auto" HorizontalAlignment="Left">
                <TextBlock Text="" Style="{StaticResource LabelStyle}"/>
                <TextBlock Text="Nom :" Style="{StaticResource LabelStyle}"/>
                <TextBlock Text="Symbole :" Style="{StaticResource LabelStyle}"/>
                <TextBlock Text="Dimension :" Style="{StaticResource LabelStyle}"/>
                <TextBlock Text="Facteur :" Style="{StaticResource LabelStyle}"/>
                <TextBlock Text="Offset :" Style="{StaticResource LabelStyle}"/>
                <TextBlock Text="Exposant :" Style="{StaticResource LabelStyle}"/>
                <TextBlock Text="Système :" Style="{StaticResource LabelStyle}"/>
                <TextBlock Text="Préfixe :" Style="{StaticResource LabelStyle}"/>
            </UniformGrid>

            <!-- BaseUnits en colonnes -->
            <ScrollViewer Grid.Row="2" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding BaseUnitViewModels}" Margin="100,0,0,0">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <local:BaseUnitColumn 
                                DataContext="{Binding}"
                                Width="150"
                                Margin="5,0"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Border>
</UserControl>